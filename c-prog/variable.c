#include <stdio.h>
#include <stdbool.h>

int main() {
    // topic: variable & datatype

    int age = 34;
    printf("User is %d year old.\n", age);

    float marks = 83.5;
    printf("User scored %.1f marks.\n", marks);

    char letter = 'E';
    printf("Letter variable has %c\n", letter);

    bool isWinter = false;
    printf("Is it winter? : %d", isWinter);

    return 0;
}